name: Auto Commit x10 (no storage)

on:
  schedule:
    # Chạy 10 lần/ngày (UTC → tương đương 7h, 10h, 13h... VN)
    - cron: '0 0 * * *'
    - cron: '0 3 * * *'
    - cron: '0 6 * * *'
    - cron: '0 9 * * *'
    - cron: '0 12 * * *'
    - cron: '0 15 * * *'
    - cron: '0 18 * * *'
    - cron: '0 21 * * *'
    - cron: '0 23 * * *'
    - cron: '30 23 * * *'
  workflow_dispatch:

jobs:
  auto-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create empty commit
        run: |
          git config user.name "prompttocode"
          git config user.email "namvu3121@gmail.com"
          git commit --allow-empty -m "🔥 Keep streak alive $(date +'%Y-%m-%d %H:%M:%S')" || echo "No commit created"
          git push

name: Auto Commit x10 (no storage)

on:
  schedule:
    # Chạy 10 lần/ngày (theo giờ Việt Nam)
    - cron: '0 0 * * *'   # 07:00 VN
    - cron: '0 3 * * *'   # 10:00 VN
    - cron: '0 6 * * *'   # 13:00 VN
    - cron: '0 9 * * *'   # 16:00 VN
    - cron: '0 12 * * *'  # 19:00 VN
    - cron: '0 15 * * *'  # 22:00 VN
    - cron: '0 18 * * *'  # 01:00 VN
    - cron: '0 21 * * *'  # 04:00 VN
    - cron: '0 23 * * *'  # 06:00 VN
    - cron: '30 23 * * *' # 06:30 VN
  workflow_dispatch: # cho phép bấm chạy tay

jobs:
  auto-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Create empty commit
        run: |
          git config user.name "prompttocode"
          git config user.email "namvu3121@gmail.com"
          git commit --allow-empty -m "🔥 Keep streak alive $(date +'%Y-%m-%d %H:%M:%S')" || echo "No commit created"
          git push
